{{ sections.settings.title }}

{% schema %}
  {
    "name": "Header",
    "tag": "header",
    "class": "header",
    "limit": 1,
    "settings": [
      {
        "type": "text",
        "id": "title",
        "label": "Titlte",
        "default": "Welcome to our title"
      }
    ],
    "blocks": [
      {
        "name": "block",
        "type": "text",
        "settings": [
          {
            "type": "text",
            "id": "heading",
            "default": "Welcome to our announcement",
            "label": "Heading"
          }
        ]
      }
    ]
  }
{% endschema %}