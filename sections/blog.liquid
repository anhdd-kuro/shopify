{% for article in blog.articles %}
  <h2>
    <a href="{{ article.url }}">{{ article.title }}</a>
  </h2>
  <div>
    タグ：
    {% for tag in article.tags -%}
      <span>{{ tag }}({{ tag.total_count }})</span>
    {%- endfor %}
  </div>
{% endfor %}
